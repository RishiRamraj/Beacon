[package]
name = "beacon-config"
description = "User settings for Beacon"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
toml.workspace = true
